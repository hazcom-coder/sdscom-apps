@model OpenSDS.Models.DOMItem	
@using OpenSDS.Models

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Chapter</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                Chapters
            </th>
            <th></th>
        </tr>
    
    @foreach (DOMItem item in Model.SubItems) 
	{
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>               
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) 
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
